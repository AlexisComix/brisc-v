//! This is a fake VM-like thing for an architecture I have made (or will be making) up.
use anyhow::{anyhow, *};

mod hardware;

fn main() -> anyhow::Result<()> {
    println!("Initialising BadRISC...");

    Ok(())
}
